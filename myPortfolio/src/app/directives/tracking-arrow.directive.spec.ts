import { TrackingArrowDirective } from './tracking-arrow.directive';

describe('TrackingArrowDirective', () => {
  it('should create an instance', () => {
    const directive = new TrackingArrowDirective();
    expect(directive).toBeTruthy();
  });
});
